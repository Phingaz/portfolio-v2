export type TJob = {
  title: string;
  text: string;
  time: string;
};

export type TProject = {
  title: string;
  description: string;
  image: string;
  link: string;
  linkToGH?: string;
};
